{
  "name": "alphaburguer",
  "version": "1.0.0",
  "description": "Alpha Burguer - Sistema de pedidos online",
  "private": true,
  "scripts": {
    "dev": "php -S localhost:8000",
    "start": "php -S localhost:8000",
    "build": "echo 'No build needed for static files'"
  },
  "engines": {
    "node": ">=20.x"
  },
  "dependencies": {
    "vercel-php": "^0.6.2",
    "better-sqlite3": "^9.2.2"
  }
}

